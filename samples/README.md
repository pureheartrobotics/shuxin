# Voice samples

Put local test audio files here, for example:

```bash
python -m shuxin.voice.cli stt samples/demo.wav --device-id demo-device-001
```

The demo accepts the audio formats supported by FunASR and ffmpeg.
