# Core runtime dependencies for the ShuXin voice demo image.
# Keep this file after the voice-specific layers so Docker can cache them.
openai>=1.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
rich>=13.0.0
prompt_toolkit>=3.0.0
httpx>=0.27.0
pydantic>=2.0.0
jinja2>=3.0.0
requests>=2.31.0
